# Used by docker-compose.yml only
# Generate a real secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace_this_with_a_long_random_secret_before_running

# Your deployed frontend URL — used for CORS
# Local dev: leave blank (defaults to http://localhost:3000)
# Production: set to your real domain, e.g. https://app.cybershield.in
CLIENT_URL=
